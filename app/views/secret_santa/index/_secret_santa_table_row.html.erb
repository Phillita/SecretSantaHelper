<tr>
  <td>
    <%= link_to ss.name || 'Secret Santa', secret_santum_path(ss) %>
  </td>
  <td>
    <%= pluralize(ss.secret_santa_participants.size, 'Participant') %>
  </td>
  <td class="text-center">
    <%= content_tag :span, '', class: "glyphicon glyphicon-#{ss.started? ? 'ok' : 'minus'}", title: "This Secret Santa #{ss.started? ? 'has started' : 'is pending'}." %>
  </td>
  <td class="text-center">
    <%= content_tag :span, '', class: "glyphicon glyphicon-#{ss.completed? ? 'ok' : 'remove'}", title: "This Secret Santa is #{ss.completed? ? 'complete' : 'incomplete'}." %>
  </td>
  <td class="text-center">
    <%= content_tag :span, '', class: "glyphicon glyphicon-eye-#{ss.secure? ? 'close' : 'open'}", title: "This Secret Santa is #{ss.secure? ? 'locked' : 'unlocked'}." %>
  </td>
</tr>
